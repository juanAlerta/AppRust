use json::{self, JsonValue};
use std::fs;

/* Funcion de parseo a json */
/* me devuelve un json que será el que se añada el log */
fn json_parser(data: String){ // revisar referencia del parámetro 

}

/* Función que añade el json al fichero log */
/* Se pasa un objeto json que será el que se añada en el fichero */
fn write(json: JsonValue){ // revisar referencia del parámetro

}
